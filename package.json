{
  "name": "asbeatcloudmailer",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/server.js"
  },
  "author": "Your Name <your.email@example.com>",
  "license": "MIT",
  "description": "A serverless email verification service using Express, Nodemailer, and Gmail API deployed on Vercel.",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.0.0",
    "express": "^4.18.2",
    "googleapis": "^150.0.1",
    "nodemailer": "^7.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
